{% extends '::page-1col.html.twig' %}

{% block title %}{{ 'core.main.indexAnonymous.title'|trans }}{% endblock %}

{% block main_menu_link_homepage %} active{% endblock %}

{% block content %}
    <div class="padding20-all">

        <div class="row-fluid">
            <div class="span4">
                <img src="{{ asset('assets/img/home.jpg') }}" alt="Accueil du site étudiant de l'UTT" />
            </div>
            <div class="span8 home-intro">
                <h3>Bienvenue sur le site étudiant de l'Université de Technologie de Troyes</h3>

                <h5>
                    Découvre les 40 clubs et associations présentes à l’UTT ainsi que les services indispensables pour faciliter ton
                    intégration et ta vie d’étudiant.
                </h5>
                <h5>
                    Sport, culture ou loisirs, de nombreux évènements et activités t’attendent ... tu n’as plus qu’à choisir
                    l’association qui te correspond le mieux et il y en a vraiment pour tous !
                </h5>
                <h5>
                    <strong>
                    Tu peux retrouver sur ces liens : <a href="{{ path("page_view", {'slug':'mentions-legales'}) }}">nos mentions légales</a>, notre <a href="{{ path("rgpd") }}">page sur le RGPD</a>, <a href="{{ path("contributors") }}">l'équipe de développeurs</a> ou encore <a href="https://github.com/ungdev/etuutt">les dernières mises à jour du site etu</a>.
                    </strong>
                </h5>
                <h6>
                    Bonne visite !<br />
                    Toute l’équipe de l'UNG (association informatique de l'UTT)
                </h6>
            </div>
            <p>
            <p>Nous utilisons Matomo, un outil open source d'analytics, pour analyser (de manière anonyme) le comportement de nos utilisateurs sur le site, afin d'orienter le développement du site sur vos services préférés ! Matomo est auto hébergé sur les mêmes serveurs que le site etu, en France à l'UTT.</p>
            <iframe
                    style="border: 0; height: 200px; width: 600px;"
                    src="https://matomo.uttnetgroup.fr/index.php?module=CoreAdminHome&action=optOut&language=fr&backgroundColor=&fontColor=&fontSize=14px&fontFamily=Helvetica"
            ></iframe>
            </p>
        </div>

        <br />

        {% if etu.modules.isEnabled('events') and events|count > 0 %}
            <h4>{{ 'core.main.indexAnonymous.events'|trans }}</h4>

            <hr />

            <div class="row-fluid">
                {% for event in events %}
                    <div class="span4">
                        <a href="{{ path('events_view', {'id': event.id, 'slug': event.title|slugify}) }}" class="homepage-event">
                            <img src="{{ asset('uploads/events/'~event.id~'.png') }}" alt="{{ event.title }}" class="pull-left event-tooltip-image" />

                            <div class="homepage-event-meta">
                                <h5>{{ event.title }}</h5>

                                <small class="muted">
                                    {{ 'core.main.indexAnonymous.date'|trans({'%date%': event.begin.format('d/m/Y')}) }}
                                </small>
                            </div>

                            <div class="clear"></div>
                        </a>
                    </div>
                {% endfor %}
            </div>

            <div class="clear"></div>

            <hr />

            <p class="pull-right">
                <a href="{{ path('events_index') }}">
                    {{ 'core.main.indexAnonymous.allEvents'|trans }}
                </a>
            </p>
        {% endif %}
    </div>
{% endblock %}
