{% extends '::page-1col.html.twig' %}

{% block title %}{{ 'buckutt.main.index.title'|trans }}{% endblock %}

{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('css/bundles/buckutt.css') }}" type="text/css" />
{% endblock %}

{% block content %}
    <p>
        Bonjour {{ name }}<br />
        Tu a {{ credit }} € sur ton comtpe
    </p>
    <hr />
    <a href="{{ path('buckutt_history') }}">Historique</a> | <a href="{{ path('buckutt_reload') }}">Recharger</a> | Administrer | <a href="{{ path('buckutt_connect', {'action':'disconnect'}) }}">Deconnexion</a>
    <hr />
    {% if step == 1 %}
        <p>Actuellement, tu as un crédit de {{ credit }} € sur ton compte.<br />
            Tu souhaites effectuer un rechargement de {{ amount }} €.<br />
            Ton solde sera donc de {{ amount_total }} €.</p>
        <br />
        {{ htmlForm|raw }}
    {% else %}
        <p>
            Le montant maximum de ton compte buckutt est {{ max_amount }}.
            Tu peux donc recharger d'un maximum de {{ possible_amount }} €.

        </p>
        <form method="post" action="{{ path('buckutt_reload', {'step':1}) }}">
            <h4>Choisis le montant à recharger :</h4>
            {{ form_widget(form.amount) }}
            <div class="buckutt-reload-submit">
                {{ form_rest(form) }}
                <button type="submit" class="btn">Rechargement</button>
            </div>
        </form>
    {% endif %}
{% endblock %}